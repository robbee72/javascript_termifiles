//functions w/ parameters
function runFunction(fn, data) {
  return fn(data);
}
// assign a function as a vlaue to a variable
var myFunc = function() {
  // do something
};
const myNewGenFunc = someParam => someParam;

// return a function

function takeFirst(f, g) {
  return f;
}
