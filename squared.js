function squareNum(num){
 return num*num;
}
squareNum(3.14); // result is stored in memory
console.log(squareNum(3.14)); // just to see the result

squareNum(9);  // result is stored in memory
console.log(squareNum(9)); // just to see the result


squareNum(1);  // result is stored in memory
console.log(squareNum(1)); // just to see the result
